<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <!--
    Параметры:
    1) host - адрес подключения к БД. По умолчанию 127.0.0.1
    2) port - порт для подключения к БД. По умолчанию 5433
    3) user - пользователь БД
    4) password - пароль пользователя БД
    5) dbms (опционально) - СУБД. По умолчанию SQLite
  -->
	<database host="127.0.0.1" port="5433" user="admin" password="root" dbms="pgsql" />
  <!--
    Параметры:
    1) host - адрес интерфейса для этого сервиса. По умолчанию 127.0.0.1
    2) port - порт для подключения к этому сервису. По умолчанию 80
  -->
	<connection host="127.0.0.1" port="5117" />
  <!--
    Параметры:
    1) level - уровень логирования:
      0: только критические события
      1: критические и важные события
      2: критические, важные и информационные события
      3: все события
    2) path (опционально) - путь к директории для записи лог-файлов. По умолчанию в текущий
    3) destination (опционально) - назначение логирования, возможно несколько значений:
      console: логирование в консоль
      file: логирование в файл
      silence: логирование отключено, исключает все остальные назначения
  -->
	<logger level="3" path="" destination="console"/>
</configuration>